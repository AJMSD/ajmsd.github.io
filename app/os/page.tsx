export default function OsModePage() {
  return (
    <main className="mx-auto flex min-h-screen w-full max-w-5xl flex-col gap-4 p-8">
      <h1 className="text-3xl font-semibold">ajmsdOS Mode</h1>
      <p className="text-zinc-300">
        Terminal interface scaffold is active. Boot sequence, commands, and virtual filesystem are
        planned in M2.
      </p>
    </main>
  );
}
