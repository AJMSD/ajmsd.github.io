export default function GameModePage() {
  return (
    <main className="mx-auto flex min-h-screen w-full max-w-5xl flex-col gap-4 p-8">
      <h1 className="text-3xl font-semibold">Game Mode</h1>
      <p className="text-zinc-300">
        Embedded game scaffold is active. Phaser integration and offline-first station flow are
        planned in M4.
      </p>
    </main>
  );
}
